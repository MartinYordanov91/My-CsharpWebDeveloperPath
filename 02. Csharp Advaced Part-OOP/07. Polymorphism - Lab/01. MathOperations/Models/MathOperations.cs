namespace Operations.Models
{
    public class MathOperations
    {
        public int Add(int x , int c)
            => x + c;

        public double Add(double x , double c ,double d)
            => x + c + d;

        public decimal Add (decimal x, decimal c, decimal d)
            => x + c + d;
    }
}
